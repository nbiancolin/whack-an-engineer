module mouse_mapper(KEY, SW, CLOCK_50, PS2_CLK, PS2_DAT, LEDR);
	input [3:0] KEY;
	input [9:0] SW;
	input CLOCK_50;
	inout PS2_CLK, PS2_DAT;
	output [9:0] LEDR;

endmodule

module mouse (resetn, start, CLOCK_50, PS2_CLK, PS2_DAT, leftclick, x, y, cState);